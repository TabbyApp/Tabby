# Postgres connection (required). For local dev with docker-compose (port 5433 avoids conflict with local postgres): postgresql://tabby:tabby@localhost:5433/tabby
DATABASE_URL=postgresql://tabby:tabby@localhost:5433/tabby

# Receipt OCR. Options: mock | google | documentai | tabscanner | mindee
#   mock       – no key; deterministic sample (dev only)
#   google     – Cloud Vision API (API key); raw text + our parsing (format-dependent)
#   documentai – Document AI Invoice/Receipt processor (service account); structured output
#   tabscanner – TabScanner API (API key)
#   mindee     – Mindee V2 extraction API (API key + model ID); structured receipt schema
# RECEIPT_OCR_PROVIDER=mock
# GOOGLE_CLOUD_VISION_API_KEY=...
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# DOCUMENT_AI_PROJECT_ID=... DOCUMENT_AI_LOCATION=us DOCUMENT_AI_PROCESSOR_ID=...
# TABSCANNER_API_KEY=...
# MINDEE_API_KEY=... MINDEE_MODEL_ID=...

# Plaid (Sandbox). Get keys from https://dashboard.plaid.com/developers/keys
# Use the Sandbox secret for local development (fake banks, no real money).
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_sandbox_secret

# Optional: frontend URL for invite links (default: http://localhost:3000)
# FRONTEND_URL=http://localhost:3000

# Optional: Twilio Verify for SMS OTP (without these, dev returns code in API response and logs it)
# Get these from https://console.twilio.com/
# Create a Verify Service at: Verify → Services → Create new service
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_VERIFY_SERVICE_SID=VA...
